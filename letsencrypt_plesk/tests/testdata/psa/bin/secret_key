#!/bin/bash

case "$1" in
    "--create") echo -n "3c4941c1-890b-5690-0c44f037ed1c" ;;
    "--delete") ;;
    ""|"--help") echo "Usage: $(basename $0) --create" ;;
    *) echo "Invalid argument: $1" && exit 1 ;;
esac
